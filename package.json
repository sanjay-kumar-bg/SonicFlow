{
  "name": "sonicflow",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --prefix ./sonicFlow-BackEnd && npm install --prefix ./sonicFlow-FrontEnd && npm install --prefix ./admin && npm run build --prefix ./sonicFlow-FrontEnd && npm run build --prefix ./admin"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
