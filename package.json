{
  "name": "sonicflow",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm install --prefix ./sonicFlow-BackEnd && npm install --prefix ./sonicFlow-FrontEnd && npm install --prefix ./admin && npm run dev --prefix ./sonicFlow-FrontEnd && npm run dev --prefix ./admin"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
